const Display = (tip, onSubmit) => {
    const grandTotal = 0;
    const total = 0;
    const aggregatedTip = 0;
    
    return <h3>You pay ${Number(grandTotal)} (${Number(total)} + ${Number(aggregatedTip)} tip)</h3>
};

export default Display;